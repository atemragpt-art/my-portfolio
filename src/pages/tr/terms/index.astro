---
/**
 * Страница-заглушка для tr языка
 * Перенаправляет на английскую версию (fallback)
 */
import { getLocalizedPath } from '@/i18n/utils';

return Astro.redirect(getLocalizedPath('/terms/', 'en'), 307);
---
